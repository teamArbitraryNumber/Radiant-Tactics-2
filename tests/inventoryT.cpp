/*#include "gtest/gtest.h"
#include "../header/inventory.h"

TEST(InventoryTest, Setsize){
    Inventory inv;
    inv.setInvSize(5);
    EXPECT_EQ(5, inv.getInvSize());
}

TEST(InventoryTest, AddWeapon){
    Inventory inv;
    inv.setInvSize(5);
    inv.addWeapon("Sword");
    inv.addWeapon("Axe");
    inv.addWeapon("Bow");
    inv.addWeapon("Magic");
    inv.addWeapon("Spear");
    EXPECT_EQ(5, inv.getInvSize());
    EXPECT_EQ(5, inv.getInventoryCounter());
}

TEST(InventoryTest, AddPotion){
    Inventory inv;
    inv.setInvSize(5);
    inv.addPotion("Small Potion");
    inv.addPotion("Large Potion");
    inv.addPotion("Healing Potion");
    inv.addPotion("Mana Potion");
    inv.addPotion("Strength Potion");
    EXPECT_EQ(5, inv.getInvSize());
    EXPECT_EQ(5, inv.getInventoryCounter());
}

TEST(InventoryTest, HasWeapon){
    Inventory inv;
    inv.setInvSize(5);
    inv.addWeapon("Sword");
    inv.addWeapon("Axe");
    inv.addWeapon("Bow");
    inv.addWeapon("Magic");
    inv.addWeapon("Spear");
    EXPECT_EQ(true, inv.hasWeapon("Sword"));
    EXPECT_EQ(true, inv.hasWeapon("Axe"));
    EXPECT_EQ(true, inv.hasWeapon("Bow"));
    EXPECT_EQ(true, inv.hasWeapon("Magic"));
    EXPECT_EQ(true, inv.hasWeapon("Spear"));
}
*/

